---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
  href: string;
  title: string;
  label: string;
  cover: ImageMetadata;
  coverAlt: string;
}

const { href, title, label, cover, coverAlt } = Astro.props;
---

<article class="case-card">
  <div class="media">
    <Image
      src={cover}
      alt={coverAlt}
      widths={[348, 696]}
      sizes="(min-width: 48rem) 336px, 100vw"
      loading="lazy"
    />
  </div>
  <p class="card-label">{label}</p>
  <h3>
    <a href={href}>{title}</a>
  </h3>
</article>

<style>
  /* Measured from the reference's project cards: 4:3 image, category, then title. */
  .case-card {
    position: relative;
    display: grid;
    gap: 2px;
    border-radius: var(--radius-image);
  }

  .media {
    margin-block-end: 14px;
    overflow: hidden;
    border-radius: var(--radius-image);
    background: var(--color-surface);
  }

  img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    transition: scale var(--duration-base) var(--ease-standard);
  }

  .case-card:hover img {
    scale: 1.03;
  }

  .card-label {
    color: var(--color-text-muted);
  }

  h3 {
    font-size: var(--text-lg);
    line-height: 1.5;
  }

  h3 a {
    color: var(--color-text);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-standard);
  }

  /* The whole card is clickable; the link's name stays the title. */
  h3 a::after {
    position: absolute;
    inset: 0;
    content: '';
  }

  h3 a:focus-visible {
    outline: none;
  }

  .case-card:has(a:focus-visible) {
    outline: 3px solid var(--color-focus);
    outline-offset: 4px;
  }

  .case-card:hover h3 a {
    color: var(--color-accent);
  }
</style>
